class Menu {
  constructor() {
    this.menu = document.querySelector(".menu");
    this.menuButton = document.querySelector(".menu__button");
  }


  /**
   * Listen to keyboard event for "M" key to toggle the menu
   * Toggle function for hiding/showing the menu
   * Animate the menu in/out
   */
}

export default Menu;